name: 'app'

type: 'java:8'

#hooks:
#    build: |
#        # Generate a unified Jar file of the application.
#        mvn package

relationships:
    database: 'mysqldb:mysql'

web:
    upstream:
        socket_family: tcp
        protocol: http
    commands:
        start: "sleep 60"
    locations:
        /:
            passthru: true

disk: 512
